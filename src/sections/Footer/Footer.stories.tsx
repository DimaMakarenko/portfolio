import { Meta, Story } from '@storybook/react';

import { Footer } from '.';

export const Default: Story = () => {
  return <Footer />;
};

export default {
  title: 'Components/Footer',
  component: Footer,
} as Meta;
