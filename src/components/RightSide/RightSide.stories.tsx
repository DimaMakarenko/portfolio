import { Meta, Story } from '@storybook/react';

import { RightSide } from '.';

export const Default: Story = () => {
  return <RightSide />;
};

export default {
  title: 'Components/RightSide',
  component: RightSide,
} as Meta;
